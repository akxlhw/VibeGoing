# v0.1.0 发布评审与验收记录

| | |
|---|---|
| 版本 | v0.1.0（M0 内核验证） |
| 评审日期 | 2026-09-06 |
| 发布 tag | `v0.1.0` → 提交 `73e5bcc`（骨架提交，含 0.1.0 版本号） |
| 评审人 | Dev（ZCode）执行，PO（akxlhw）确认 |
| 依据 | [RELEASE_PLAN.md](../RELEASE_PLAN.md) §3 checklist、[PLAN.md](../../PLAN.md) §4 M0 验收 |

## 1. 范围冻结

M0 故事范围：VG-003（核心对话路径测试）状态**完成**；M0 无其他在册故事，无 `待验收` 项。
范围外（明确不属于本版本）：VG-001 CI、VG-002 质量基线（Sprint 1 进行中，随 v0.2.0 发布）。

## 2. M0 验收标准逐条核对

| 验收项 | 结果 | 证据 |
|---|---|---|
| Soul 存储（人设/原则/模型绑定 → 本地 JSON） | ✅ | `test_soul_store_roundtrip`：保存→列表→加载字段一致；`test_cli_list_souls`：CLI 可列 |
| TeammateFlow 会话（每轮记忆召回注入/回复沉淀） | ✅ | `test_conversation_turn_roundtrip`：多轮 user/assistant 历史正确；`test_system_prompt_injects_soul_identity`：身份注入 |
| 记忆系统本地构建（LanceDB + scope 隔离） | ✅ | 发布前手工验证：`Memory 构建成功: Memory | root_scope = vibegoing`；`test_memory_scope_naming` |
| 无记忆路径可用（记忆为增强项不阻塞对话） | ✅ | `test_teammate_without_memory` |
| 终端 REPL 入口 | ✅ | `uv run vibegoing`（M0 验收时人工演示）；`main()` 可编程调用 |
| BYO Key（LiteLLM provider 字符串） | ✅ | `.env.example` 提供 VIBE_LLM 与多 provider 示例（ADR 0005） |
| 冒烟测试无需 API Key 全绿 | ✅ | 下列 §3 测试证据（替身 LLM，零密钥依赖） |
| Windows 依赖链可用 | ✅ | Windows 11 + Python 3.12：uv sync 全 wheel 安装，无编译失败 |

## 3. 质量门禁证据（2026-09-06，本地，与 CI 同口径）

```
$ uv run pytest          → 9 passed（4 例 M0 冒烟 + 5 例发布补充）
$ uv run ruff check .    → All checks passed!
$ uv run black --check . → 6 files would be left unchanged
$ uv run mypy            → Success: no issues found in 4 source files
$ uv build               → vibegoing-0.1.0.tar.gz + vibegoing-0.1.0-py3-none-any.whl
```

版本一致性守卫：`test_version_consistency` 校验 pyproject / `__version__` / 安装元数据三处均为 0.1.0。

## 4. 发布物

- Git tag：`v0.1.0`（已推送）
- GitHub Release：v0.1.0（Notes 取自 CHANGELOG [0.1.0] 段，附 wheel/sdist）
- 安装包：`uv tool install vibegoing` 或从 Release 附件安装

## 5. 已知限制（随后续版本解决）

| 限制 | 影响 | 解决故事 |
|---|---|---|
| 会话不跨进程持久化（一次运行一个会话） | 重启丢短期上下文（长期记忆仍在） | VG-101（v0.2.0） |
| 记忆依赖 OpenAI embedding | 无 OpenAI Key 时记忆功能不可用（对话不受影响） | VG-107 |
| 回复非流式 | 长回复需等待 | VG-104（v0.2.0） |
| GitHub Actions 首跑状态待仓库端确认 | 私有仓库需在 Actions 标签页启用 | VG-001 收尾（S1 评审项） |

## 6. 评审结论

M0 全部验收项通过，质量门禁全绿，版本一致性与发布物齐备。**建议发布 v0.1.0**；PO 确认后本记录即为验收凭据，未决项（§5）已全部登记在册。
